spring:
  application:
    name: api-gateway


## API-GATEWAY CONFIG


  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/orders/**
        - id:  inventory-service
          uri: lb://INVENTORY-SERVICE
          predicates:
              - Path=/inventory/**
